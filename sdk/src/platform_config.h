/*
 * platform_config.h — net_v_app（ZynqMP A53 + PS GEM3）
 *
 * 与 SDK lwip_echo_server 模板为本硬件自动生成的内容一致
 * （见模板 data/lwip_echo_server.tcl 的 swapp_generate）。
 */
#ifndef __PLATFORM_CONFIG_H_
#define __PLATFORM_CONFIG_H_

#define PLATFORM_EMAC_BASEADDR XPAR_XEMACPS_0_BASEADDR

#define PLATFORM_ZYNQMP

#endif
